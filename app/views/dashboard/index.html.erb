<br/>
<span class="pull-left">
    <%= form_tag charges_path do %>
      <article class="text-center">
        <label class="amount">
          <% if !current_user.credits.first.nil? %>
          <% current_user.credits.each do |credit| %>
            <p class="color">My credit:  <%= credit.value%> </p>
            <% end %>
            <% else %>
             <p class="color">My credits: <b> 0 </b> </p>
        <% end %>
          <span class="h5">Add $1.00</span>
        </label>
      </article>

      <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
              data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
              data-description="A month's subscription"
              data-amount="100">
      </script>
    <% end %>
    </span>
   <span class="h2 col-md-4 col-md-offset-3 ">Dashboard</span>
<div class="dash text-center">
 
</div>